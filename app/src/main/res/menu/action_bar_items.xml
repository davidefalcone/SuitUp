<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">


    <item
        android:id="@+id/filtra"
        android:icon="@drawable/filter_icon"
        android:title="@string/filtra"
        app:showAsAction="always"/>

    <item android:id="@+id/aggiungi_capo"
        android:icon="@drawable/ic_action_name"
        android:title="@string/aggiungi_capo"
        app:showAsAction="always"/>

    <item android:id="@+id/account_settings"
        app:actionLayout="@layout/account_image"
        android:title="@string/account_settings"
        app:showAsAction="always"/>

</menu>